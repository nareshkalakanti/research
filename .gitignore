# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# generated reports
/output/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — keep .env.example as the public template
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# SQLite — commit *.db only; never copy or commit WAL sidecars between machines
**/*.db-wal
**/*.db-shm
*.db-wal
*.db-shm

# Accidental SQLite opens without .db suffix
/data/companies
/data/companies-shm
/data/companies-wal

# Local empty / timestamped DB backups
**/*.empty-bak*
**/data/*.empty-bak*

# corrupt SQLite backups
**/*.corrupt*
*.corrupt*

# Local PDF uploads for Concall Research
/data/concall-uploads/

# Large local sample PDFs / scratch extracts (keep out of git)
/data/samples/*.pdf
/data/samples/*.extract-draft.md
/data/samples/*-combined.txt

# Cursor local (rules/skills may be committed selectively)
# /.cursor/
